{
  "type": "module",
  "scripts": {
    "build:engine": "cd Mythsensus && npx tsc --target ES2020 --module CommonJS --outDir build --strict false --esModuleInterop true --skipLibCheck true --noEmitOnError false report-engine/lib/calc.ts report-engine/lib/report.ts && node tests/bundle-engine.cjs && node -e \"require('fs').copyFileSync('build/ms26-bundle.js','../build/ms26-bundle.js')\"",
    "test:engine": "node Mythsensus/tests/test-pk-chu-26sys.cjs",
    "qa": "node Mythsensus/tests/qa-scanner.js",
    "deploy": "git push origin main && vercel --yes --prod"
  },
  "dependencies": {
    "@vercel/og": "^0.11.1",
    "stripe": "^17.4.0"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "typescript": "^5.9.3"
  }
}
