.vercel
.claude/
node_modules/
# QA + simulator artifacts (regenerated on demand)
qa-screenshots/
qa-report.html
user-sim-screenshots/
user-sim-report.html
audit-bilingual-screenshots/
audit-bilingual-report.json
_credentials.local.md
# Session-scoped QA / one-shot artifacts — may contain API tokens, never commit.
_qa-out/
# Design mockups / option-comparisons used during a session; not shipped to prod.
_mockups/
# Design / cold-skeptic audit screenshots, regenerable on demand.
_audit-screens-*/
# Backup snapshots written by data-enrich + inject scripts before destructive ops.
data/gods.json.pre-*.bak
build/ms26-bundle.js.pre-*.bak
# Secrets — vercel env pull writes .env / .env.* here; NEVER commit.
.env
.env.*
# Pre-deploy backup snapshots (large; keep local only).
_backups/

# ── เพิ่ม 2026-08-23 (ตอนเก็บงานที่ขึ้นเว็บไปแล้วแต่ไม่เคยเข้า git) ──────────
# ⛔ .gitignore ไม่รองรับคอมเมนต์ท้ายบรรทัด — ทั้งบรรทัดจะถูกอ่านเป็น pattern
#    คอมเมนต์ต้องอยู่บรรทัดของตัวเองเท่านั้น (พลาดมาแล้วรอบนึงวันนี้)

# งานทดลองโมเดล 3 มิติ 693 MB — หนักเกินกว่าจะอยู่ในประวัติ
_god3d-pilot/
# สแนปช็อตก่อนเขียนทับ (กฎเดิมครอบแค่ gods.json ไม่ครอบ gods-lore.json)
data/gods-lore.json.pre-*.bak
# ภาพ mockup / ภาพตรวจ สร้างใหม่ได้จากสคริปต์
_mock-*.png
_verify-*.png
_guardian-cards.png
# Supabase CLI เขียนเอง
supabase/.temp/
# bts-rally = คนละโปรเจกต์ (rally-solver) แค่บังเอิญมาวางในโฟลเดอร์นี้
bts-rally/

# ภาพร่างหน้าจอ + สคริปต์เรนเดอร์ที่ใช้ระหว่างออกแบบ ไม่ได้ขึ้น prod
_mock-*.html
_render-*.cjs
_verify-*.cjs
_guardian-audit.cjs
_guardian-card-proto.html
# ข้อมูล lore ที่สคริปต์ generate ออกมา สร้างใหม่ได้
_seo-gen/
# Python cache
__pycache__/
*.pyc
Mythsensus/tests/walk-*.json

# ผลผลิตชั่วคราวของต้นแบบคำอ่านรายศาสตร์ — ไฟล์ raw/prompt ที่ประกอบใหม่ได้
_oneoff/_in*.txt
_oneoff/*.raw.txt
_oneoff/nul
_oneoff/err*.txt
_oneoff/compose2.html
_oneoff/compose3.html
_oneoff/oneoff-bazi-v2.html
_qa-blind/
