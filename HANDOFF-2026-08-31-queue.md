# คิวงาน Mythsensus — ส่งต่อ 31 ส.ค. 2026

เซสชันนี้หยุดแล้วตามที่ director สั่ง (*"ไม่ให้ทำ session นี้ มีอีก session ที่ทำ mythsensus
เหมือนกัน ส่งงานไปเป็น queue"*) **ไม่มีไฟล์ค้างในเครื่อง** — commit ล่าสุด `2ca74fd` บน
branch `engine/cosmic-blueprint-audit` ทำงานต่อได้ทันที

---

## ⛔ อ่านก่อนแตะอะไร — สามข้อที่ทำให้วันนี้เสียเวลาไปครึ่งวัน

**1. prod ไม่ได้พัง และไม่เคยพัง**
ตรวจสดวันนี้ เดินครบ flow บน https://mythsensus.com → `data-group=free` `data-sub=pulse`
`has-chart=true` `panel-pulse` 563 ตัวอักษร แท็บครบ js errors 0

**การทดสอบหน้าเว็บนี้มี 3 ขั้น ไม่ใช่ 1** — กดปุ่มทอง → กรอกฟอร์ม (`entryHour`, `entryCity`)
→ **ติ๊ก `entryAgeConfirm` (13+)** → กดปุ่มที่ `onclick` มี `entryAccept()`
ถ้าหยุดที่ขั้นแรกจะเห็น panel ว่างกับไม่มีแท็บ ซึ่งหน้าตาเหมือน outage เป๊ะ
**สองเซสชันสรุปผิดแบบเดียวกันวันนี้ รวมทั้งเซสชันนี้เอง** — ตัวอย่างที่ทดสอบถูกอยู่ใน
`Mythsensus/tests/smoke-reading.cjs`

**2. `_backups/2026-08-26-report-audit/ROLLBACK.md` มีเงื่อนไข ⛔ อยู่หัวไฟล์แล้ว — อย่ารันถ้าไม่ครบ**
วันนี้มีเซสชันรันมัน ทำให้ `report.ts` ถอยกลับไปก่อน audit (BaZi กลับมาโหวต 2 ครั้งขัดกันเอง ·
ป้าย `ระดับเทียบประชากร` ที่โกหกว่าเทียบประชากรโลกกลับมา) และ **ลบด่าน 2 ตัวทิ้ง**
กู้แล้ว แต่อย่าให้เกิดซ้ำ — สาเหตุคืออ่านสีแดงของเทสต์แล้วสรุปว่าสินค้าพัง

**3. `npm run deploy` deploy จาก "โฟลเดอร์ในเครื่อง" ไม่ใช่จาก git**
⇒ ของที่ยังไม่ commit ของ**ทุกเซสชัน**จะขึ้น prod ไปด้วย · วันนี้เกิดแล้ว 1 ครั้ง
(`git add -A` ของเซสชันหนึ่ง กวาดงานของอีกเซสชันที่จดไว้ว่า "ยังไม่ deploy" ขึ้นไป)
**เช็ค `git status` ก่อนกดทุกครั้ง**
⚠️ และมันรัน `git push origin main` แบบ hardcode — ถ้าอยู่ branch อื่น เว็บจะขึ้นแต่ git ไม่ตาม

---

## สถานะจริง ณ ตอนนี้

| | |
|---|---|
| branch | `engine/cosmic-blueprint-audit` · commit `2ca74fd` · **tree สะอาด** |
| `origin/main` | `58d240b` — **ตามหลัง 2 commit** (`bfe60d5` ของอีกเซสชัน + `2ca74fd` ของเซสชันนี้) |
| prod | `?v=202` ปกติ · smoke ทั้งสองผ่าน |
| ด่าน | `report-invariants` · `system-mechanics` · `prose-density` เขียวหมด (อยู่ใน `predeploy` แล้ว) |

---

## คิวงาน — เรียงตามที่ควรทำ

### 1. ตัวอย่าง "สมุดโน้ต" แนวตั้ง — **นี่คืองานที่ director รออยู่**
สั่งไว้ว่า *"ทำตัวอย่างให้ดูก่อน"* ยังไม่ได้ทำ

**โจทย์:** แถบ "ดูดวงรายครั้ง" มี 14 ปุ่มเรียงแนวนอน ต้องเลื่อน **2.3 เท่าความกว้างจอ**
(วัดจริงบน 390px) ⇒ ของ 13 ชิ้นแทบไม่มีใครเห็น
director เสนอเอง: *"ทำเป็นหน้าใหม่ไปเลยว่า กดเข้ารายเดือนแล้ว มี tab ลงมาว่าจะดูอะไร
อารมณ์แบบ side tab สมุดโน๊ต"*

**ข้อเสนอที่ยังไม่ได้ยืนยัน:** แนวตั้งเฉพาะ "รายครั้ง" (14 ชิ้น เห็นครบในจอเดียว + ใส่ราคาได้)
ส่วน "รายเดือน" (7 ชิ้น) คงแนวนอนเพราะสมาชิกสลับบ่อย ต้องไว

- ราคาจริงอยู่ที่ `const PRICING` ใน `index.html` — **ห้ามแต่งเลข** ($19/$9/$7/$5)
- ทำเป็น `_mock-*.html` แยกไฟล์ (`.vercelignore` กันไว้แล้ว ไม่ขึ้นเว็บ ไม่ชนใคร)
- **ถ่ายภาพหน้าจอจริงส่ง director** ก่อนแตะ `index.html` — DOM ผ่านแต่ภาพจับได้ เคยเกิดมาแล้ว

### 2. push ให้ `main` ตามทัน prod
`origin/main` ตามหลัง 2 commit ⇒ ถ้าใครเช็คเอาต์ main สะอาดแล้ว deploy **เว็บจะย้อนกลับ**
ทั้งสอง commit เป็นของดีและ verify แล้ว: `bfe60d5` (H1 + JSON-LD ที่ยังขายคำว่า
"reconciled into one Cosmic Score" ทั้งที่เอนจินเลิกเฉลี่ยแล้ว) · `2ca74fd` (nav 3 หมวด + smoke test)
`git merge-base --is-ancestor main HEAD` ผ่าน = fast-forward ได้ ไม่มี conflict

### 3. เก็บกวาดที่ค้าง
- `Mythsensus/tests/inject-bundle-root.cjs` abort เสมอ — `index.html` โหลด bundle เป็น
  external script แล้ว · ขั้นตอนใน repo `CLAUDE.md` ที่บอกให้รัน **ล้าสมัย** ควรลบทิ้ง
- ตาราง Gumroad ใน repo `CLAUDE.md` ล้าสมัย (ชื่อสินค้าจริงตอนนี้คือ
  `Mythsensus — Full Report (in-depth PDF)` ไม่ใช่ `43-page PDF`) — เคยทำให้เซสชันนี้
  บอก director ผิดมาแล้ว **query API ก่อนเชื่อตาราง**

---

## ⏸️ รอ director เคาะ — อย่าตัดสินใจแทน

1. **สเกล Cosmic Score 300–999** — ค่าจริงทั้งประชากรกว้าง **71 แต้ม** แต่ยืดแสดงเป็น 700
   ⇒ ต่างกัน 4% กลายเป็นคนละชั้น (Dawn vs Radiance) ตัวเลขซื่อสัตย์แล้วแต่สเกลยังโอเวอร์
2. **ไทยพราหมณ์นับวันตามปฏิทิน** ทั้งที่ตำราไทยเปลี่ยนวันตอนพระอาทิตย์ขึ้น
   ⇒ คนเกิดก่อนรุ่งสางได้เทพ/สี/ทักษาผิดวัน · แก้แล้วรายงานลูกค้าเก่าเปลี่ยนทุกคน
3. **ความยาวรายงาน 44 หน้า** — ที่มันยาวเพราะคำสั่ง director เอง 27 ส.ค.
   (*"ทุกศาสตร์ควรลึกเท่ากัน"*) ⛔ **ห้ามตัดกลับเงียบๆ**
   ทางเลือกที่เสนอไว้: (ก) ยุบภาค C เหลือการ์ด ~20 หน้า · (ข) แยก A+B ~12 หน้า + ภาคผนวก C ·
   (ค) ลดความหนาแน่นต่อหน้า
4. **เกณฑ์ `sharedProsePct` = 23.5%** ใน `prose-density.test.cjs` เป็น **ratchet**
   (ลดได้ ห้ามเพิ่มเพื่อดับแดง) — 20% เดิมเป็นเลขที่ตั้งเองไม่มีที่มา director ยังไม่เคาะเป้าจริง

---

## บทเรียนวันนี้ที่ควรถือติดตัว

- **เทสต์ต้องวัดคำสัญญา ไม่ใช่ชื่อ element** — `smoke-reading` ผูกกับ `#panel-forecast`
  พอ director เปลี่ยนสินค้ามันเลยแดง แล้วสีแดงนั้นทำให้คนถอยงานทิ้ง
  ตอนนี้มันถามหน้าเว็บว่า panel ไหน active แล้วเช็คว่ามีเนื้อจริงไหม → เปลี่ยน default กี่ครั้งก็ตามไปเอง
- **คู่มือ rollback คือปืนที่วางไว้** ถ้าไม่เขียนว่า "เมื่อไหร่ห้ามรัน" เดี๋ยวมีคนหยิบ
- **ตรวจงานที่มีหน้าตาต้องถ่ายภาพจริง** — เลข DOM บอกว่า 21 ปุ่มเรียบร้อยดี
  แต่ภาพบอกว่าเห็นแค่ 2-3 ปุ่ม ที่เหลือตกขอบจอ
