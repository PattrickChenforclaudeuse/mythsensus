# Mythsensus — Handoff Brief (Session 2 of 2026-05-02)

**Date:** 2026-05-02 (continuation of [HANDOFF-2026-05-02.md](HANDOFF-2026-05-02.md))
**Live URL:** <https://mythsensus.com>
**Latest commit deployed:** `c73cc4a`

---

## ⚡ TL;DR — what changed this session

7 commits across 4 themes, all deployed:

1. **qa-scanner gap fix** — scanner caught pageerrors but exited 0; now fails the run on uncaught JS / HTTP ≥400 / raw HTML leak. New `tests/local-test-server.cjs` so the scanner can be tested against an edited working tree.

2. **Marketing-page alignment with per-input $9 pricing** — pricing/support/privacy/index.html drifted from the in-app `PRICING` constant after the `4c45fc2` rollout. Fixed: dropped subscription tier and $14.99 bundle, swapped Lemon Squeezy → Omise, replaced "FREE BETA · NO PAYMENT YET" banner with honest "SOFT-LAUNCH BETA · prices live, gateway pending".

3. **Visual QA pass + content fixes** — iterative scan-fix-scan loop with new `tests/interactive-qa-scan.cjs` (walks every group/sub-tab, captures layout metrics + screenshots). Fixed two real bugs: (a) `<br>` swallowed without separator in deep-reading secondary tiles caused `貪狼ระบบดาราศาสตร์` jamming, (b) 94 `font-family` declarations missing CJK / Thai system fallback chain. False positives in scanner cleaned up over iterations.

4. **In-app i18n + UX fixes** — `applyLang()` re-renders side rails + blessing history on language toggle (was leaving rails Thai while center went English). Deep-reading `<p>` paragraph spacing restored after global `*{margin:0}` reset. Social login buttons (LINE / Facebook / Google) scaffolded in entry overlay — wired to `_sb.auth.signInWithOAuth()`, gracefully fail until Supabase dashboard is configured. **`_BETA_ALL_UNLOCKED = true`** flag bypasses every paywall during the no-Omise window so mobile users can actually read the deep content.

---

## ✅ Done this session (7 commits)

| # | Commit | What it shipped |
|---|---|---|
| 1 | `dad2ac0` | qa-scanner now exits 1 on pageerror / HTTP ≥400 / raw HTML leak. Adds `tests/local-test-server.cjs` static helper. |
| 2 | `aa52ce5` | pricing/support/privacy aligned with per-input $9 model. Lemon Squeezy → Omise. Banner re-honestified. |
| 3 | `18cdd86` | Removed 14 orphan `ao_*_price` strings (old subscription/$14.99-bundle model, 0 DOM refs). |
| 4 | `123f29b` | Two real visual bugs: `<br>`-strip jamming CJK + Thai (deep secondary tiles now `貪狼 · ระบบดาราศาสตร์` with separator) + 94 `font-family` declarations got `'Songti SC','PingFang SC','Microsoft YaHei','Sarabun'` fallback so multilingual content harmonises across scripts. New `tests/interactive-qa-scan.cjs` + `tests/probe-{ziwei-br,chevron}.cjs` regression helpers. |
| 5 | `c89e045` | `applyLang()` now re-renders `renderTodayBar()` + `renderHistory()` so side rails + blessing list follow language toggle (were frozen post-boot). |
| 6 | `7869e86` | Three-in-one: deep-reading `<p>` paragraph spacing CSS, LINE/FB/Google `_socialSignIn()` helpers + buttons in entry overlay, EN translation Phase 1 (cover page + partial p_threeScores via new `tr(th, en)` helper). New `tests/inject-bundle-root.cjs` to inject built engine bundle into root `index.html` (the existing `inject-bundle.js` only targeted offline-app HTML). |
| 7 | `c73cc4a` | `_BETA_ALL_UNLOCKED = true` constant → `_hasItemAccess()` short-circuits `true` for everyone during the no-Omise window. Mobile users no longer stuck behind paywalls. |

---

## 📌 Carried-over context (still important)

### Memory files updated this session
- `project_pricing_decision.md` — defer A/B/C subscription pivot until first-100-buyer conversion data
- `project_canonical_pricing.md` — `index.html:8573` is the source-of-truth `PRICING` constant; marketing pages drift; processor is **Omise**, never **Lemon Squeezy**
- `project_easter_egg_unknown_tier.md` — the `???` row in GOD COLLECTION is intentional 1-in-a-million Easter egg, do not "fix" it

### Visual QA convergence loop result (from session)
After all real bugs fixed + scanner false-positive filters in place, the interactive scan reports `overlaps=0 / fontDrift=0 / clipped=0` across 28 captured tab views (5 groups × 2 viewports + 4 premium subTabs + 9 subscription subTabs + 8 addon subTabs). Remaining `mixedScripts` entries are intentional multilingual content (system titles like `紫微斗數 — เพื่อน (交友)`, Sanskrit mantras, etc.) all with proper separators after the `<br>` fix.

---

## 🚧 Queue — what's pending for next session

### A. EN translation Phase 2 — ~3 hrs (BIG)

Phase 1 in `c73cc4a` shipped:
- `tr(th, en)` helper in `Mythsensus/report-engine/lib/report.ts` line 53
- `p01_cover` (page 1, 100% translated)
- `p_threeScores` (page 2, ~60% translated — Soul Frequency big display + Petroleum analogy + Top contributors header done; Bottom contributors footer + closing notice still Thai-only)

**EN is dormant** — `MS26.calculate()` callers in `index.html` don't pass `lang:` parameter, so `chart.input.lang` is undefined → `_lang` defaults to `'th'` per `report.ts:1815`. Users see no behavior change yet.

What's left:
1. Translate `p02_scoreBreakdown` through the 18 `p_*` functions (saju, tibetan, ziwei, onmyodo, hellenistic, norse_rune, ogham, arabic_parts, kabbalistic, zoroastrian, aztec, native_american, ifa_yoruba, aboriginal, biorhythm, vedic_mahadasha, etc.)
2. Translate the 16 `p_*_new` system pages (lines ~1965+)
3. Wire `lang: LANG` into every `MS26.calculate({...})` call in `index.html` (currently 9+ call sites — grep `window.MS26.calculate` to find them all)
4. Run `npm run build:engine` + `node tests/inject-bundle-root.cjs` to ship the rebuilt bundle into `index.html`
5. Test: render with `lang:'en'` against a fixed test chart, eyeball the PDF output

Brand voice notes for translator: keep system names in romanisation (Saju / 紫微斗數 / Onmyōdō kept as-is), translate the surrounding Thai narrative. Petroleum Grade analogy is core brand voice — keep "Petroleum / Engine / Fuel Line" mapping intact in EN. Don't paraphrase Pattrick's voice into corporate-speak.

### B. Supabase OAuth config — user action (~30 min, no Claude Code involvement)

Frontend already has the buttons (`_socialSignIn('line' | 'facebook' | 'google')`). Until the providers are enabled in Supabase dashboard, the buttons throw `auth.signInWithOAuth` errors and fall through to the bilingual "Sign-in failed — please try another method" toast.

Project ref: `woamqrhifuxsscnihqco`
Callback URL (paste at every provider console): `https://woamqrhifuxsscnihqco.supabase.co/auth/v1/callback`

**What user needs to do:**

1. **URL Configuration:** https://supabase.com/dashboard/project/woamqrhifuxsscnihqco/auth/url-configuration
   - Site URL: `https://mythsensus.com`
   - Redirect URLs: `https://mythsensus.com/?login=1`, `https://mythsensus.com/`, `https://mythsensus.com/**`
2. **Providers:** https://supabase.com/dashboard/project/woamqrhifuxsscnihqco/auth/providers
   - LINE — register LINE Login channel at https://developers.line.biz/console/, paste Channel ID + Channel secret. Enable OpenID Connect. Apply for Email permission.
   - Facebook — register app at https://developers.facebook.com/, paste App ID + App Secret.
   - Google — create OAuth client at https://console.cloud.google.com/, paste Client ID + Client Secret. Configure OAuth consent screen first.

User declined Claude in Chrome extension setup ("ไม่มี auto bypass ต้องนั่งกดอนุมัติทุกครั้ง"), so they'll do this manually. Detailed step-by-step is in the previous session's chat history.

### C. Other smaller items (low priority, deferred)

- `js/translations.js` and `Mythsensus/Offline app/mythsensus-offline.html` still have the same 14 orphan `ao_*_price` strings the root `index.html` had. Not user-visible (translations.js isn't loaded; offline-app is a separate distribution) but dead code. Cleanup is a 1-min job whenever someone touches those files.
- The mojibake `.bak` files at `Mythsensus/report-engine/lib/{calc,report}.ts.mojibake.bak` are leftover artifacts from a charset migration. Can be deleted if nobody is using them as references.

---

## ⏳ External blockers — Pattrick actions, NOT Claude Code

| Blocker | Action |
|---|---|
| Omise approval | Pending Omise account review. When approved → swap `_purchaseItem()` `confirm()` stub → `OmiseCard.open()` (plan in `brief-handoff-expressive-platypus.md` Task 2) → flip `_BETA_ALL_UNLOCKED = false` in `index.html` to re-engage the paywall. |
| Supabase OAuth provider apps | LINE / Facebook / Google developer consoles (see section B above). |
| Supabase OTP E2E on prod | Manual sign-out → close tab → return → verify session restores as `<email>`. Reported pending in earlier handoff. |
| `VERCEL_TOKEN` in GitHub secrets + workflow | If we want to deploy from CI instead of local `npm run deploy`. Optional. |

---

## 🐛 Known engine bugs (deferred)

Carried over from earlier handoff — none touched this session:

- BaZi Day Pillar anchor offset wrong → Day Master may be wrong on every chart (Feb 3, 1991 → 甲午 vs expected 丙午)
- Month Pillar Lichun day check bug (Feb 3, 1991 → 丙寅 vs expected 己丑)
- Library entries display as "PAST CHART · revert inputs to view" UX

---

## 🧠 Tribal knowledge (carry forward)

- `score.cosmicFinal` is the canonical Cosmic Score (cover + summary boxes use this)
- HD Authority is pseudo-deterministic — NOT a proper defined-centers calculation
- `?login=1` URL parameter triggers OTP overlay deep-link
- `applyLang()` re-renders the active panel via the `setGroup(CUR_GROUP)` + `showSubTab(CUR_SUB)` dispatch at the bottom — **panels added newer than 2026-05-02 don't need separate wiring**, they pick up LANG via the dispatch automatically (this session added `renderTodayBar` + `renderHistory` for the same reason).
- Engine source of truth: `Mythsensus/report-engine/lib/{calc,report}.ts` → `npm run build:engine` → `Mythsensus/build/ms26-bundle.js` → `node tests/inject-bundle-root.cjs` → re-injects into `index.html` between `MS26_BUNDLE_START` / `MS26_BUNDLE_END` markers.
- Inline `<script>` syntax-error trap: a single `'` inside a single-quoted template literal closes the string early → 1.2 MB script crashes silently. The patched qa-scanner now catches this via the `[pageerror]` listener + `process.exitCode = 1`. Test it by reverting commit `3a29bd6` and running `BASE_URL=http://127.0.0.1:8124 npm run qa` — should exit 1.
- During the no-Omise window, `_BETA_ALL_UNLOCKED = true` lets every user past every paywall. Don't forget to flip this back to `false` when Omise lands. Grep `_BETA_ALL_UNLOCKED` to find it (single source of truth in `index.html`).

---

## 📂 References

- [HANDOFF-2026-05-02.md](HANDOFF-2026-05-02.md) — earlier session today
- [COMPETITOR-RESEARCH.md](COMPETITOR-RESEARCH.md) — Co-Star / Pattern / Sanctuary / TAN TAROT
- `brief-handoff-expressive-platypus.md` (referenced from earlier session, not in this working tree)

---

## 💬 Recommended opening prompt for next session

> Repo at `D:\Claude works here\Mythsensus`. Read [HANDOFF-2026-05-02-v2.md](HANDOFF-2026-05-02-v2.md). Last deployed commit `c73cc4a` is live at https://mythsensus.com.
>
> First task: **EN translation Phase 2** — finish translating the remaining ~40 `p_*` functions in `Mythsensus/report-engine/lib/report.ts` using the existing `tr(th, en)` helper. After translating, wire `lang: LANG` into the 9+ `window.MS26.calculate({...})` call sites in `index.html`, then `npm run build:engine` and `node Mythsensus/tests/inject-bundle-root.cjs` to ship. Test by setting `mth_lang=en` in localStorage and generating a chart.
>
> Don't touch `_BETA_ALL_UNLOCKED` — that's deliberate until Omise lands.

Don't open the new chat with "ทำต่อ" — Claude will just ask. Hand it the specific first task from the handoff.
