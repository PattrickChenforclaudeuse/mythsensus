# ใบส่งงาน — Mythsensus 23–24 ส.ค. (ภาษา + หน้าตา)

> เขียนไว้ที่นี่ตามที่เซสชันประสานงานขอ (ห้ามแตะ `TIMELINE.md` / `MEMORY.md` เอง)
> **สถานะ: commit ในเครื่องแล้ว `0ed63a9` · ยังไม่ push ยังไม่ deploy** — รอ director เคาะ

## 🔴 รอ director ตัดสิน 2 เรื่อง (ห้ามแก้เอง)

1. **"26 ศาสตร์" vs โรสเตอร์จริง 27** — `taksa` ให้คะแนนในพยากรณ์แต่ไม่มีชื่อใน `system-rules.json` (26 คีย์)
   ⇒ 9 โหวต + 18 งดออกเสียง = 27 ขณะที่เว็บเขียน 26 อยู่ 19 จุด
   แก้ชั่วคราวแล้ว: หน้าพยากรณ์บอกยอดรวม ("9 จาก 27") เลขจะได้ไม่ขัดกันเอง — **ยังไม่ได้แก้แบรนด์**
   ทางเลือกจริง: เปลี่ยนแบรนด์เป็น 27 · หรือยุบ taksa เข้าเสียงอื่น (คะแนนจะเปลี่ยน)
   หลักที่ director เคยวางไว้กรณี 太歲: *"ยอดรวมห้ามขัดคำโฆษณา"*

2. ⛔ **7 ใน 9 ศาสตร์ให้คะแนนเท่ากันหมดทั้ง 4 ด้าน** — วัดจากดวงจริง:
   `Vedic 3333 · BaZi 2222 · มายัน 2222 · ทักษา 3333 · Biorhythm 222`
   มีแค่ **เลขศาสตร์ (3321)** กับ **โหราศาสตร์ตะวันตก (244)** ที่แยกตามด้านจริง
   ⇒ ความต่างระหว่าง 4 ด้านมาจาก 2 ศาสตร์ + การถ่วงน้ำหนัก ไม่ใช่จาก 9 เสียง
   **เว็บขายว่าประเมิน 4 ด้านแยกกัน** — ต้องเคาะว่ายอมรับตามตำรา (ศาสตร์นั้นมีสัญญาณเดียวจริง) หรือทำให้แยกด้านจริง
   *ตรวจซ้ำได้:* `node -e "global.window=global;require('./build/ms26-bundle.js'); ..."` (นับคะแนนรายศาสตร์ข้ามด้าน)

## ✅ ที่ทำไปแล้ว — ภาษา (ตรวจ 6 รอบ)

reviewer คนละตัวทุกรอบ ไม่รู้ว่าใครเขียน · **ผลทุกรอบ: ไทย HUMAN (~90%) · อังกฤษ PROBABLY HUMAN (~75%)**

⛔ **สิ่งที่เจอส่วนใหญ่เป็นบั๊กจริง ไม่ใช่เรื่องสำนวน:**
- **คำแนะนำชุดปิด 8 คำ มีถ้อยคำเดียวต่อคำ ⇒ ประโยคเดียวโผล่ทุกหมวด** จน `เปิดเกม ลงมือเรื่องใหญ่ได้` ไปอยู่ใต้สุขภาพ 1/5 เหนือคำแนะนำ "พักก่อน"
  → **key ยังเป็นชุดปิด (จึงยังนับ "ตรงกัน" ได้) แต่ถ้อยคำแยกตามหมวด** = `FORECAST_ADVICE_BY_DOMAIN` + `MS26.fcAdviceFor(domain, key)` · override เฉพาะที่ผิด ไม่มีก็ fallback
- `เกิด 3 ก.พ. 2534 พ.ศ.` — **พ.ศ. ต้องอยู่หน้าปี**
- แถวเสียงค้านเขียน "ตรงกับข้างบน" ทั้งที่บรรทัดเหนือมันคือ **ผู้ให้คะแนนสูงสุด** = ตรงข้าม
- การ์ดแชร์เขียน `/ 10` และคำอธิบายเขียน "out of 10" ทั้งที่สเกลเป็น 1–5 ไปแล้ว
- พาดหัวซ้ำ 3 ชั้นติดกัน · `systems`/`traditions` ปนกันฝั่ง EN · `GREEK MYTHOLOGY` ค้างบนฝั่งไทย
- **ฟอร์มขั้นสองเป็นไทยล้วนบนฝั่ง EN** — `_entryRefreshLang()` ทำงานนี้ได้ครบอยู่แล้ว แค่ไม่เคยถูกเรียกจาก `applyLang()`

⚠️ **ผมทำพังเองระหว่างแก้ 2 ครั้ง แล้วรอบถัดไปจับได้ทั้งคู่** (`เจ้าปีคืออังคารเป็นเจ้าปี` · "ตรงกับข้างบน" ชี้ผิดบรรทัด)
⇒ **ตรวจซ้ำหลังแก้ทุกครั้ง อย่าหยุดที่รอบเดียว**

**ไม่แก้:** reviewer เสนอ `Relationship`→`Love`, `Wealth`→`Money` — **director สั่ง Wealth/Relationship เอง** คำสั่ง director ชนะ

## ✅ ที่ทำไปแล้ว — หน้าตา (จากรายงานเซสชันเพื่อน)

| # | เรื่อง | สถานะ |
|---|---|---|
| 1 | ปุ่มลอย EN/TH ทับ `庚午 ธาตุโลหะ·ม้า` บนมือถือ | ✅ ซ่อนปุ่มลอยตอน overlay เปิด |
| 2 | สลับภาษามี 2 ชุดพร้อมกัน | ✅ กฎเดียวกันแก้ทั้งคู่ |
| 3 | เดสก์ท็อปเป็นคอลัมน์กลางจอ | ❌ **ไม่ทำ** — ที่เห็นคือ *modal เปิดหน้าแรก* วางกลางจอมืด เป็นแพตเทิร์นปกติของ overlay ไม่ใช่คอลัมน์มือถือยืดมา · รื้อ layout เดสก์ท็อปไม่มีใครขอ (Rule #8) · **หน้าแอปหลังปิด overlay ยังควรตรวจแยก** |
| 4 | CTA ตัดบรรทัดเหลือ "ฟรี" โดด | ✅ ย่อคำ + `text-wrap:balance` |
| 5 | ตัวหนาลากข้าม 2 บรรทัด | ✅ ย่อสิ่งที่ทำตัวหนา + `white-space:nowrap` |
| 6 | ปุ่มหลัก/รอง จบด้วย "· ฟรี" ทั้งคู่ | ✅ ปุ่มรองเป็น "รับพรวันนี้ก่อนก็ได้" |

**วิธีแก้ข้อ 1+2:** มี 8 จุดที่เปิด/ปิด overlay และจะมีจุดที่ 9 ⇒ ใช้ `MutationObserver` สะท้อนสถานะเป็น `body.entry-open` แทนการไล่แก้ทีละจุด
⚠️ **ตรวจแล้วว่าปุ่มลอยกลับมาตอนปิด overlay** — ไม่งั้นแอปหลักจะไม่เหลือสวิตช์ภาษาเลย

## 🧪 เครื่องมือที่ทิ้งไว้ (scratchpad ของเซสชันนี้ — หายเมื่อปิด ให้ก๊อปถ้าจะใช้ต่อ)

- `dump-copy.cjs` — ดึงข้อความที่คนอ่านจริงทั้ง 2 ภาษาออกเป็น JSON
- `shot-site.cjs` — ถ่าย 2 ขนาด + **วัดว่าอะไร fixed/sticky ทับข้อความอะไรบ้าง** (อย่าตัดสินด้วยตาที่ 1×)
- `shot-card.cjs` — เรนเดอร์การ์ดแชร์ออกเป็น PNG แล้วเปิดดู · ⚠️ ต้องติ๊กช่องอายุ 13+ ก่อน ไม่งั้น `entryAccept()` เงียบ
- `check-pill.cjs` — ยืนยันปุ่มลอยกลับมาตอนปิด overlay

## ⛔ กับดักที่เจอ ห้ามเหยียบซ้ำ

- **canvas ไม่มี `font-variant-numeric`** — Cormorant Garamond ตั้งเลขโบราณเป็นค่าเริ่มต้น การ์ดเลยออกมาเลข 2/3/7 จมใต้เส้นบรรทัดทั้งใบ · แก้ด้วยการ**เอาฟอนต์ที่มีเลขปกติไว้หน้าสุดใน stack** (fallback เป็นราย glyph ไทยจึงไม่เปลี่ยน) · `document.fonts.ready` อย่างเดียวไม่พอ
- **`npm run build:engine` เท่านั้น** — เรียก `bundle-engine.cjs` เดี่ยวๆ จะไม่ compile TS แล้วได้บันเดิลเก่าเงียบๆ
- **ตรวจงานที่มีหน้าตาต้องเปิดรูปดูจริง** — สถิติพิกเซลบอกได้แค่ "รูปวาดลงไปแล้ว" มันไม่เห็นว่าชื่อเทพชนบรรทัดล่าง
- **`try/catch` กลืน ReferenceError เงียบ** — เขียน `isTh` ใน `applyLang` ที่ไม่มีตัวแปรนั้น = ฟอร์มไม่แปลเลยโดยไม่มี error
